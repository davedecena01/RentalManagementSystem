export const environment = {
  production: true,
  apiUrl: '',           // Set via Vercel env var at build time
  supabaseUrl: '',      // Set via Vercel env var
  supabaseAnonKey: '',  // Set via Vercel env var
  stripePublishableKey: ''
};
